using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(BaseMinigameUI))]
public class BaseMiniGameUIEditor : Editor
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
